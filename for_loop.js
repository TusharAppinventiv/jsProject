let num = prompt("Enter a number :");
for(let i = 1 ; i <= 10 ; i++){
    //for loop is a single-line command that will be executed repeatedly
    let n = num * i;
    console.log(num, "*", i , "=", n);
}
